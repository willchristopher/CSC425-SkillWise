{
  "name": "skillwise_aitutor",
  "version": "1.0.0",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "start": "./start.sh",
    "stop": "./stop.sh",
    "dev": "concurrently \"npm start --prefix backend\" \"npm start --prefix frontend\" --names \"backend,frontend\" --prefix-colors \"blue,green\"",
    "dev:all": "docker-compose up -d && echo 'Dev environment is up! Frontend: http://localhost:3000 | Backend: http://localhost:3001/api'",
    "down": "docker-compose down -v",
    "logs": "docker-compose logs -f",
    "logs:backend": "docker-compose logs -f backend",
    "logs:frontend": "docker-compose logs -f frontend",
    "logs:db": "docker-compose logs -f database",
    "build": "docker-compose build",
    "rebuild": "docker-compose build --no-cache",
    "reset": "docker-compose down -v && docker-compose up -d",
    "test": "npm test --prefix backend",
    "test:unit": "npm test --prefix backend -- tests/unit/",
    "test:integration": "npm test --prefix backend -- tests/integration/",
    "cypress": "cypress open",
    "cypress:run": "cypress run",
    "test:e2e": "cypress run",
    "prepare": "husky install"
  },
  "keywords": [
    "ai",
    "tutor",
    "education",
    "react",
    "node",
    "postgresql"
  ],
  "author": "",
  "license": "ISC",
  "description": "SkillWise AI Tutor - An intelligent tutoring system",
  "devDependencies": {
    "@testing-library/cypress": "^10.1.0",
    "cypress": "^15.6.0",
    "eslint": "^8.44.0",
    "husky": "^8.0.3",
    "lint-staged": "^13.2.3",
    "prettier": "^2.8.8"
  }
}
